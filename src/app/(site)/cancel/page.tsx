import Cancel from "@/components/layout/cancel/Cancel";
import PageWrapper from "@/components/utils/PageWrapper";
import React from "react";

const Page = () => {
  return (
    <PageWrapper className="bg-light-ivory">
      <Cancel />
    </PageWrapper>
  );
};

export default Page;
